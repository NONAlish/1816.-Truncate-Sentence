/**
 * @param {string} s
 * @param {number} k
 * @return {string}
 */
var truncateSentence = function(s, k) {
    let newS = s.split(' ')
    let newArr = [];
    for (let i = 0; i < k; i++) {
        newArr[i] = newS[i] 
    }

    return newArr.join(' ')

};
